# CS:APP Data Lab  
# 教师说明

**2011 年 5 月 31 日：现已包含 “Beat the Prof” 竞赛**

版权所有 © 2002-2018，R. Bryant 和 D. O'Hallaron

---

## 简介

本目录包含运行 CS:APP Data Lab 所需的文件。该实验旨在帮助学生理解以下内容：

- 位级表示
- 补码算术
- IEEE 浮点数表示

此外，我们还提供了一个新的、基于 HTTP 的用户级 “Beat the Prof” 竞赛，用来替代旧的基于电子邮件的版本。

这个新竞赛是完全自包含的，不需要 root 密码。唯一要求是：你需要在一台具有 IP 地址的 Linux 机器上拥有一个用户账号。

系统要求：

- 使用 `bison`
- 使用 `flex`

它们用于构建 `dlc`。

---

# 1. 概述

在本实验中，学生需要完成一个名为 `bits.c` 的 C 文件。

该文件由一系列编程“谜题”组成。每个谜题都是一个空的函数体，学生需要补全它，使其实现指定的数学功能，例如：

- 绝对值
- 位运算
- 补码相关操作
- 浮点数相关操作

对于非浮点数题目，学生必须使用：

- 直线型 C 代码
- 受限制的 C 算术运算符
- 受限制的逻辑运算符

对于浮点数题目，学生可以使用：

- 条件语句
- 任意运算符

学生使用以下三个工具检查自己的工作。教师也使用相同工具进行评分。

---

## 1.1 `dlc`

`dlc` 是一个 “Data Lab Compiler”，即 Data Lab 编译器。

它会检查 `bits.c` 中每个函数是否符合编码规则，包括：

- 是否使用了超过限制数量的运算符
- 是否只使用直线型代码
- 是否只使用合法运算符

实验中包含了 `dlc` 的源代码和 Linux 二进制文件。

---

## 1.2 `btest`

`btest` 是一个测试框架，用于检查 `bits.c` 中函数的正确性。

该工具已经得到了显著改进，现在会对整数和浮点数表示中的边界情况进行大范围测试，例如：

- `0`
- `Tmin`
- 非规格化数与规格化数的边界
- 正无穷和负无穷

---

## 1.3 `driver.pl`

`driver.pl` 是一个自动评分驱动程序。

它会使用：

- `dlc`
- `btest`

来检查 `bits.c` 中每个测试函数的：

- 正确性
- 是否遵守编码规范

---

## 1.4 默认题目

Data Lab 的默认版本包含 15 个谜题。

这些谜题位于：

```bash
./src/selections.c
